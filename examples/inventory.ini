# Replace with your host. The role assumes Docker + the compose plugin are
# already installed (your infra repo handles that; for a standalone test,
# install Docker first).
[playground]
your-server ansible_host=192.168.0.10 ansible_user=ansible
